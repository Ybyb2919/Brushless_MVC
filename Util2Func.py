from datetime import datetime
from typing import Optional, List

class CommandFunc:
    time: datetime
    motor_id: int
    pos_move: int  # movement from original position
    initial_kp: Optional[float]
    initial_kd: Optional[float]
    func_level: int  # 1-5 defining different movement functions

def mov_func(self, circ , movKp, movKd):
    for x in range(steps):
        time = time - 2
        pos_move =
        func_commands.append(Command_Func(time, pos_move, initial_kp, initial_kd, func_level)
    10 steps
    each step different command
    divide the remaining position points between the amount of steps
    time - divide the time between all
    for loop running each iteration as a different step


def movFastPrecise:

def movFastWabble:

def movSlowPrecise:

def movSlowWabble

def mov



    def mov_two:

    def mov_three:

    def mov_four:

    def mov_five:
